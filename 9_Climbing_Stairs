class Solution {
    public int climbStairs(int n) {
        if(n==1)return 1;
        if(n==2)return 2;
        int count=0;int one=1,two=2;
        for(int i=3;i<=n;i++){
            count=one+two;
            one=two;
            two=count;
        }
        return count;
    }
}
